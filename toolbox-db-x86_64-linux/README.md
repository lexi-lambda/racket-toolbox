# toolbox-db-x86_64-linux

This package provides a version of `libsqlite3.so` built with `SQLITE_ENABLE_STMT_SCANSTATUS`, which allows `query` and related functions from `toolbox/db/base` to report query plans.
